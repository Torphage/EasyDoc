/**
 * $[Description]
 *$|
 * @${EXPORT}|$$|
 * @${ACCESS}|$$|
 * @${ABSTRACT}|$$|
 * @${CONST} ${NAME}|$$|
 * @param$| {${PARAMS_TYPES}}|$ ${PARAMS} $[Description]|$$|
 * @returns {${RETURN_TYPE}} $[Description]|$$|
 * @${RELATION} {${RELATIONNAME}}|$$|
 * @memberof ${PARENT}|$
 */