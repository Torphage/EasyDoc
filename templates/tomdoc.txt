# Description: $[Type]
# 
# Args:
#     ${args.var} {${args.type?}}: \${$[args]}
# 
# Examples:$<3>(
#     ${func.name}($[param1], $[param2])
#     # => $[result])
# 
# Returns:
#     The ${return.type?}
